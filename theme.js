"use client";
import { createTheme } from "@mantine/core";

export const theme = createTheme({
  primaryColor: "blue",
  fontSmoothing: true,
  breakpoints: {
    xs: '5em',
    sm: '5em',
    md: '64em',
    lg: '74em',
    xl: '90em',
  },
  
});
